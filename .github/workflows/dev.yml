name: Dev

on:
  pull_request:
    branches: [Dev]
  push:
    branches: [Dev]
  workflow_dispatch:

concurrency:
  group: dev-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  ci:
    name: CI
    uses: ./.github/workflows/_reusable-ci.yml

  required:
    name: Dev required
    if: ${{ always() }}
    needs: [ci]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Verify required jobs
        env:
          CI_RESULT: ${{ needs.ci.result }}
        run: test "$CI_RESULT" = "success"

  merge-to-dev:
    name: Enable automatic merge to Dev
    if: ${{ github.event_name == 'pull_request' && needs.required.result == 'success' }}
    needs: [required]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Enable auto-merge for the validated pull request
        env:
          GH_TOKEN: ${{ secrets.PROMOTION_TOKEN }}
        run: |
          gh pr merge "${{ github.event.pull_request.number }}" \
            --repo "${{ github.repository }}" \
            --auto \
            --merge

  promote:
    name: Promote Dev to NonProd
    if: ${{ github.event_name == 'push' && needs.required.result == 'success' }}
    needs: [required]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    concurrency:
      group: environment-promotion
      cancel-in-progress: false
    steps:
      - name: Create or find promotion pull request
        env:
          GH_TOKEN: ${{ secrets.PROMOTION_TOKEN }}
        run: |
          PR_NUMBER=$(gh pr list \
            --repo "${{ github.repository }}" \
            --base "NonProd" \
            --head "Dev" \
            --state open \
            --json number \
            --jq '.[0].number')

          if [ -z "$PR_NUMBER" ]; then
            PR_URL=$(gh pr create \
              --repo "${{ github.repository }}" \
              --base "NonProd" \
              --head "Dev" \
              --title "Promote Dev to NonProd" \
              --body "Automated promotion from \`Dev\` to \`NonProd\`. This PR merges after all NonProd requirements pass.")
            PR_NUMBER=${PR_URL##*/}
          fi

          echo "Promotion PR #$PR_NUMBER will be validated and merged by the NonProd workflow."
